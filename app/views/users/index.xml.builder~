xml.instruct!
@users = User.all 

xml.users do 	
xml.all do
	@users.each do |user|  
		xml.user do
		xml.user_id user.id
		xml.firstName user.firstName
		xml.lastName user.lastName
		xml.emailAddress user.emailAddress
		xml.phoneNumber user.phoneNumber
		end
	end 
end
end




